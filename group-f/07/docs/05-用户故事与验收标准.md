# 05 — 用户故事与验收标准

| 属性 | 内容 |
|------|------|
| **模块编号** | M4 |
| **模块名称** | 团队协作 · 舆情/趋势与多渠道推送 |
| **文档包版本** | v1.2 |
| **技术栈** | 前端 React · 后端 Flask · 代码 GitHub |
| **开发方式** | CoPaw + 本模块 Spec |
| **模块侧重** | 消息获取 + 舆情/趋势规则 + 钉钉/飞书/邮件；Git 分支分工与合并规范。 |

---

## US-M4-001（P0）规则 CRUD

| 字段 | 内容 |
|------|------|
| **编号** | US-M4-001 |
| **故事** | 作为运营，我希望增删改推送规则并绑定渠道，以便管理投放策略。 |
| **AC** | 1) `POST/GET/PATCH/DELETE /notify/rules` 行为与 `09` 一致；2) `channelIds` 非法返回 **400**；3) 禁用规则 `dispatch` 可返回 **409** `M4_RULE_DISABLED`（若实现）。 |
| **REQ** | REQ-M4-001 |

## US-M4-002（P0）试发与历史

| 字段 | 内容 |
|------|------|
| **编号** | US-M4-002 |
| **故事** | 作为运营，我希望试发并查看投递历史，以便确认触达与审计。 |
| **AC** | 1) `POST /notify/dispatch` 默认 `dryRun=true` 时 **不** 调用外网渠道（或调用沙箱）；2) `GET /notify/deliveries` 含 `traceId`、`status`；3) `dryRun=false` 时产生可核验的外发或沙箱回执。 |
| **REQ** | REQ-M4-002 |

## US-M4-003（P0）渠道测试

| 字段 | 内容 |
|------|------|
| **编号** | US-M4-003 |
| **故事** | 作为运营，我希望测试渠道连通性，以便上线前校验配置。 |
| **AC** | `POST /notify/channels/{type}/test` 返回 `ok` 或明确错误；**不得** 在响应中泄露完整 webhook 密钥。 |
| **REQ** | REQ-M4-003 |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-04-04 | 按 M4 生成 |
| v1.2 | 2026-04-04 | US 对齐 `09` |
